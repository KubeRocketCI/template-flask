apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pyt.fullname" . }}-test-connection"
  labels:
    {{- include "pyt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pyt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
